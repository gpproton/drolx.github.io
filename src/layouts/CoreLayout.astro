---
import { SITE_AUTHOR, SITE_DESCRIPTION, SITE_TITLE } from "@/consts";
import { ClientRouter } from "astro:transitions";
import "../assets/app.css";

interface Props {
  title: string;
  description?: string;
  image?: string;
  class?: string;
}

const isProduction = import.meta.env.PROD;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const { title, description, image, class: clazz } = Astro.props;
const { url, site } = Astro;
const openGraphImageUrl = new URL(
  image ?? "/images/og-image.png",
  Astro.url,
).toString();
const tagId = import.meta.env.TAG_ID;
const tagManagerId = import.meta.env.PUBLIC_TAG_MANAGER_ID;
const layoutTitle = `${title} :: ${SITE_TITLE} | ${SITE_DESCRIPTION}`;
---

<!doctype html>
<html lang="en">
  <head>
    <title>{layoutTitle}</title>
    <ClientRouter />
    <!-- Favicon  -->
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" href="/logo.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/apple-touch-icon-180x180.png" />
    <!-- Primary Meta Tags -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta name="title" content={layoutTitle} />
    <meta
      name="description"
      content={description ?? `${title} - ${SITE_DESCRIPTION}`}
    />
    <meta name="generator" content={Astro.generator} />
    <meta name="author" content={SITE_AUTHOR} />
    <meta name="copyright" content="drolx-source-1.0" />
    <meta
      name="keywords"
      content="drolx, software, tech, development, software solution, cloud services, cloud security, sass, product development, devOps, logistics, retail, telematics, gps tracking"
    />
    <meta name="color-scheme" content="light" />
    <meta name="theme-color" content="#281588" />
    <meta property="og:url" content={`${site}${url}`} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={layoutTitle} />
    <meta property="og:image" content={openGraphImageUrl} />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:site" content="@drolxLab" />
    <meta name="twitter:title" content={layoutTitle} />
    <meta name="twitter:image" content={openGraphImageUrl} />
    <!-- Others -->
    <link rel="canonical" href={canonicalURL} />
    <link rel="sitemap" type="application/xml" href="/sitemap-0.xml" />

    <!-- Analytics Scripts -->
    <script type="text/partytown" is:inline>
      var _paq = (window._paq = window._paq || []);
      _paq.push(["setDocumentTitle", document.domain + "/" + document.title]);
      _paq.push(["setCookieDomain", "*.drolx.com"]);
      _paq.push(["setDomains", ["*.drolx.com"]]);
      _paq.push(["disableCookies"]);
      _paq.push(["trackPageView"]);
      _paq.push(["enableLinkTracking"]);
      (function () {
        var u = "//analytics.drolx.com/";
        _paq.push(["setTrackerUrl", u + "matomo.php"]);
        _paq.push(["setSiteId", "1"]);
        var d = document,
          g = d.createElement("script"),
          s = d.getElementsByTagName("script")[0];
        g.async = true;
        g.src = u + "matomo.js";
        s.parentNode.insertBefore(g, s);
      })();
    </script>
    <noscript>
      <p>
        <img
          referrerpolicy="no-referrer-when-downgrade"
          src="//analytics.drolx.com/matomo.php?idsite=1&rec=1"
          style="border:0;"
          alt="" />
      </p>
    </noscript>
    <!-- End Analytics Scripts -->
  </head>
  <body
    class:list={[
      clazz,
      "scrollbar scrollbar-w-1 scrollbar-thumb-rounded-[0.25rem]",
      "scrollbar-track-slate-200  scrollbar-thumb-gray-400",
      "dark:scrollbar-track-gray-900 dark:scrollbar-thumb-gray-700",
    ]}
  >
    <slot />
  </body>
</html>
